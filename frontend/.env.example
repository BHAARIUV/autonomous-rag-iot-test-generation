# Copy this file to ".env" (in this frontend/ directory) and adjust as needed.
# The React frontend talks to the REAL Python backend over HTTP at this base URL.
# The backend serves the JSON API via:  python -m app.dashboard --port 8877
VITE_API_BASE_URL=http://127.0.0.1:8877